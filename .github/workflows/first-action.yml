name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
            echo "hello nahid"
            echo "hello nijhor"
            echo "hello sourov"
      - name: Print goodbye
        run: echo "Done - bye!"
